<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />

        <variable
            name="user"
            type="com.untilled.roadcapture.data.entity.User" />

        <variable
            name="onClickItem"
            type="android.view.View.OnClickListener" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/constraint_ifollowing_filter_container"
        android:layout_width="92dp"
        android:layout_height="92dp">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/img_ifollowing_filter_border"
            android:layout_width="62dp"
            android:layout_height="62dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            bind:FollowingAlbumUpdate="@{user.update}" />

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/img_ifollowing_filter_profile"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:onClick="@{onClickItem}"
            android:scaleType="centerCrop"
            app:civ_border_color="@color/white"
            app:civ_border_width="1dp"
            app:layout_constraintBottom_toBottomOf="@+id/img_ifollowing_filter_border"
            app:layout_constraintEnd_toEndOf="@+id/img_ifollowing_filter_border"
            app:layout_constraintStart_toStartOf="@+id/img_ifollowing_filter_border"
            app:layout_constraintTop_toTopOf="@+id/img_ifollowing_filter_border"
            bind:FollowingAlbumProfileImage="@{user.profileImageUrl}"
            tools:src="@drawable/img_sample_thumbnail" />

        <TextView
            android:id="@+id/text_ifollowing_filter_username"
            style="@style/UsernameSmall"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="4dp"
            android:layout_marginTop="8dp"
            android:text="@{user.username}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/img_ifollowing_filter_profile"
            tools:text="kwangddang11" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>